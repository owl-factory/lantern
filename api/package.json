{
  "name": "@reroll/api",
  "version": "0.2.0",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "private": true,
  "scripts": {
    "dev": "yarn build-all && nodemon -V -w src -e ts,tsx -x \"yarn build && yarn start\"",
    "start": "netlify dev",
    "build": "tsc",
    "build-all": "lerna run build --scope=@reroll/api --include-dependencies"
  },
  "devDependencies": {
    "@types/cookie": "^0.4.0",
    "@types/express": "^4.17.8",
    "@types/mongoose": "^5.7.36",
    "netlify-cli": "^2.62.0",
    "typescript": "3.9.7"
  },
  "dependencies": {
    "@reroll/model": "0.2.0",
    "apollo-server-lambda": "^2.17.0",
    "cookie": "^0.4.1",
    "graphql": "^15.3.0",
    "mongoose": "^5.10.5",
    "nodemon": "^2.0.4",
    "type-graphql": "^1.0.0"
  }
}
