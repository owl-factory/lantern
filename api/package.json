{
  "name": "@reroll/api",
  "version": "0.2.0",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "private": true,
  "scripts": {
    "dev": "nodemon -V -w src -e ts,tsx -x \"yarn start\"",
    "start": "tsc && node -r dotenv/config dist/index.js",
    "build": "tsc",
    "build-all": "lerna run build --scope=@reroll/api --include-dependencies"
  },
  "devDependencies": {
    "@types/express": "^4.17.8",
    "@types/mongoose": "^5.7.36",
    "dotenv": "^8.2.0",
    "typescript": "~3.4.5"
  },
  "dependencies": {
    "@reroll/model": "0.2.0",
    "apollo-server-lambda": "^2.17.0",
    "graphql": "^15.3.0",
    "mongoose": "^5.10.5",
    "nodemon": "^2.0.4"
  }
}
