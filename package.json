{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "start": "lerna run start",
    "dev": "lerna run dev",
    "copy": "cp -r api/dist/* app/out_functions"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "api",
    "app",
    "model"
  ]
}
