import { ModuleDocument } from "types/documents";

export function validateNewModule(doc: Partial<ModuleDocument>) {
  return;
}

export function validateUpdatedModule(doc: Partial<ModuleDocument>) {
  return;
}
