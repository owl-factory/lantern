// Shorthand for a simple type
export type Scalar = string | number | boolean;

// A named type to represent the specific indexes that Owl Factory uses to better indicate
// when they are being used explicitly
export type Ref64 = string;
