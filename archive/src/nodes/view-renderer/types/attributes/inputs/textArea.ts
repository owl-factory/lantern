import { Expression } from "../../expression";

// Describes the attributes of a text area input
export interface TextAreaAttributes {
  className: Expression;
  id: Expression;
  name: Expression;
}
