import { Expression } from "../../expression";

// Describes the attributes of a radio input
export interface RadioAttributes {
  className: Expression;
  id: Expression;
  name: Expression;
  label: Expression;
  value: Expression;
}
