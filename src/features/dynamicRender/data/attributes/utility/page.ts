import { AttributeDefinition } from "features/dynamicRender/types/attributes/definition";

/** Attribute definitions for page elements */
export const pageAttributes: AttributeDefinition[] = [{ name: "name", required: true }];
