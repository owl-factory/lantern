import { AttributeDefinition } from "features/dynamicRender/types/attributes/definition";

/** Attribute definitions for tabs elements */
export const tabsAttributes: AttributeDefinition[] = [{ name: "for", required: true }];
