import { AttributeDefinition } from "features/dynamicRender/types/attributes/definition";

/** Attribute definitions for collapse elements */
export const collapseAttributes: AttributeDefinition[] = [{ name: "id", required: true }];
