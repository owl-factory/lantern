import { AttributeDefinition } from "features/dynamicRender/types/attributes/definition";

/** Attribute definitions for the button elements */
export const buttonAttributes: AttributeDefinition[] = [{ name: "action", required: true }, { name: "target" }];
