import { MarkupController } from "./common";

/**
 * An empty Markup Controller as a placeholder in the event that no proper
 * controller is defined.
 */
export class NullMarkupController extends MarkupController {}
