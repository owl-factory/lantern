import React from "react";
import { VscLoading } from "react-icons/vsc";

/**
 * A simple loading icon to be used in place of text when something is still being loaded in,
 * such as the name of a ruleset for a character.
 */
export function Loading() {
  return <VscLoading/>;
}
