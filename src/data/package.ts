// eslint-disable-next-line no-restricted-imports
import packageJson from "../../package.json";

/**
 * Version number string from project's `package.json` file.
 */
export const version = packageJson.version;
