import { __testing__ } from "../../controllers/ActorSheetController";

describe("new render", () => {
  it("creates a new render", () => {
    const mockController = new __testing__.ActorControllerClass();
  });
});
