import { Expression } from "../../expression";

// Describes the attributes of the checkbox
export interface CheckboxAttributes {
  className: Expression;
  id: Expression;
  name: Expression;
  value: Expression;
}
