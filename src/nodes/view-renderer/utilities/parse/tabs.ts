/**
 * Parses the tabs into an object usable for determining the current page and state
 * @param xmlDOM The XML DOM document to extract the tabs from
 * @returns An object describing the different tab groups and the different pages within
 */
export function parseTabs(xmlDOM: XMLDocument): Record<string, any> {
  const tabs: Record<string, any> = {};
  return tabs;
}
