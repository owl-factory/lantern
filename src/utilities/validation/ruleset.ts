import { RulesetDocument } from "types/documents";


export function validateNewRuleset(doc: Partial<RulesetDocument>) {
  return;
}

export function validateUpdatedRuleset(doc: Partial<RulesetDocument>) {
  return;
}
